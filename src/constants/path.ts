const path = {
  home: '/',
  role: '/role',
  user: '/user',
  profile: '/user/profile',
  changePassword: '/user/password',
  login: '/login',
  register: '/register',
  logout: '/logout'
} as const

export default path
